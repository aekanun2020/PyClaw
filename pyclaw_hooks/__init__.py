"""Ready-to-use hook implementations that ship with PyClaw.

These are *real* deterministic guardrails (principle #1), not examples. Register
them with the HookEngine via PythonRunner targets like
`pyclaw_hooks.guards:block_destructive`.
"""
