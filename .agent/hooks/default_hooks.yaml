# ⚠️ SUPERSEDED — this file is NOT read by the runtime.
#
# `config.hooks_dir` points here, but NO code consumes it. The only path that
# registers hooks at startup is PluginLoader.load_all() (cli.py:100), which
# scans `.agent/plugins/*/plugin.yaml` (loader.py:108,176). Specs placed here
# were silently never loaded — HookEngine started empty and every answer passed
# ungated (the CCTV regression: an answer citing ม.23 without retrieval was not
# blocked).
#
# The live hook set now lives in:
#     .agent/plugins/pdpa-grounding/plugin.yaml   [A4]
#
# Keep this file as documentation only. Do NOT add hook specs here expecting
# them to load — add them to a plugin manifest instead.
hooks: []
